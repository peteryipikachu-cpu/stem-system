# STEM 题目审核系统：上线前重点专项自动化测试报告
**测试时间**：2026-08-07 10:00:13
**测试环境**：`http://127.0.0.1:8000` (独立测试库 `stem_test`)
**测试执行器**：Focus Guide End-to-End Test Suite

---

## 📊 重点测试结果汇总
| 序号 | 测试重点 / 场景 | 执行状态 | 运行详情 & 验证结论 |
| --- | --- | --- | --- |
| 1 | 重点一：个人上传上限 (2/2) | **`PASSED`** | 第 3 道提交因个人上限被系统拒绝导入 (created=0, skipped=1) |
| 2 | 重点一：月任务数上限 (1/1) | **`PASSED`** | 第 2 次发起被入口 409 预检拦截: 账号已达月上限 1 次质检，无法发起新任务，下月自动恢复。 |
| 3 | 重点二：场景 A：正文裸写命令未加 $ (不通过题) | **`PASSED`** | L0 格式校验不合格阻断，命中规则 `['latex-unwrapped-math-command', 'latex-unwrapped-math-command', 'latex-unwrapped-math-command']` |
| 4 | 重点二：场景 B：标准合格表达式 (通过题) | **`PASSED`** | L0 校验顺利通过，无格式错误阻断 |
| 5 | 重点三：不通过题目全流程 | **`PASSED`** | 顺利拦截阻断，难度状态定格为 `format_failed` |
| 6 | 重点三：通过题目全流程 | **`PASSED`** | L0 校验通过，任务正常自动晋级后续评测层 |
| 7 | 重点四：重新检测增量复用 | **`PASSED`** | 重新质检瞬间复用历史已完成结果，成功避免重复上游 Token 消耗 |
| 8 | 重点五：新版本上限拦截 | **`PASSED`** | 保存新版本后提交质检被接口 409 精准拦截: 账号已达月上限 1 次质检，无法发起新任务，下月自动恢复。 |

---

## 🔍 各重点专项测试详细记录
### 1. 重点一：上传与月配额门控
- **个人上传上限 (`question_upload_limit=2`)**：测试账号在成功导入第 1、2 道题后，第 3 道提交被系统返回 HTTP 201 显式拒绝导入 (created=0, skipped=1)。
- **月质检任务数 (`monthlyRequestLimit=1`)**：测试账号第 1 次发起质检成功，第 2 次发起在后端入口被 `require_quota_available` 预检拦截返回 409。

### 2. 重点二：LaTeX 格式校验规则与阻断
- **正文裸写命令未加 `$`**：成功精准捕捉 `latex-unwrapped-math-command` 阻断错误。
- **变量与数字紧贴**：成功捕捉 `latex-subscript-missing` 阻断错误。
- **未闭合/错位符号**：成功捕捉 `latex-delimiter-mismatch` 阻断错误。
- **标准规范公式**：L0 校验顺利通过，展示绿色 Tag 自动进入下层。

### 3. 重点三：上传题目与全流程查看
- **不通过题目全流程**：包含了裸写 `\boxed` 的题目发起检测后定格为 `format_failed`。
- **通过题目全流程**：规范题目 L0 校验绿灯通过并自动发起后续各层评测。

### 4. 重点四：重新检测增量复用 (`inherit_successful_solves`)
- 对已完成题目再次触发“重新检测”时，系统瞬间继承了历史成功答案，无冗余 Redis 排队与 Token 开销。

### 5. 重点五：新版本在配额上限时的质检提交拦截
- 修改题目保存为新版本 (`currentVersion = 2`) 成功解开了合格锁定，但在当月任务配额超限时，提交质检请求被入口 409 成功拦截。