# STEM 题目审核系统 v0.0.11_3 发版说明（增量补充）

发布日期：2026-08-07（v0.0.11 增量版之后的新增内容）

## 🔌 模型接入调整

- **kimi 网关模型 ID 更名**：因上游网关旧渠道下线，`kimi-k3` 统一更名为 `kimi/kimi-k3`，前端模型目录、后端治理价目与 L3 默认策略、Worker 调用渠道三端同步更新。
- **存量配置自动迁移**：服务启动时自动把历史治理配置中的旧模型 ID 幂等更新为新 ID，升级无需人工执行 SQL，旧环境无缝兼容。

## 🔍 公式校验规则补充

- **裸希腊字母名检测**：拦截 `sigmaa` 这类丢失反斜杠的希腊字母笔误（如应为 `\sigma`）。
- **裸数学函数名检测扩充**：数学函数名字典化并扩充词表（如 `sinc` 等合法函数不误报），正文裸写函数名触发提示，降低漏报与误报。

## 🛠️ 工具与运维

- **LaTeX 校验报告工具增强**：支持按项目维度扫描题库出报告；报告正文改为纯 Markdown 输出（公式可直接渲染）；新增 Excel 模式，可按导入模板表头直接扫描题目文件。

## ✅ 质量验证

- 后端自动化测试全量通过（98/98，新增模型 ID 迁移回归用例）；Worker LaTeX 规则用例 35/35 全绿。
